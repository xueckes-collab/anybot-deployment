(function(window, $, undefined){
    var phoenixSite = window.phoenixSite || (window.phoenixSite = {});
    var name = "component_album";
    var albumList = phoenixSite.albumList || (phoenixSite.albumList = {});

    $.extend(albumList, {
        init: function() {
            
        }
    });
})(window, jQuery)